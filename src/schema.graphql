type Query {
  users: [User!]!
}

type Mutation {
  createUser(email: String!, firstname: String!, lastname: String!): User
}

type User {
  id: ID!
  email: String!
  firstname: String!
  lastname: String!
  fullname: String!
}
